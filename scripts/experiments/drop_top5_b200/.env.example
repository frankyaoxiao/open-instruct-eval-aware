# Copy to .env, fill in real values.
# Both open-instruct (during training) and fortress (during eval) will source this.

# Used by:
#   - open-instruct LLM-judge reward during training (for code/general-quality verifiers)
#   - fortress eval-awareness scorer (GPT-5-mini judges each completion)
OPENAI_API_KEY=

# Used to download allenai/Olmo-3-7B-Think-DPO and allenai/Dolci-Think-RL-7B from the Hub
HF_TOKEN=
